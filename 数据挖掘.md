# 音频数据挖掘项目执行文档

  

## 1. 项目背景与选题动机

  

随着流媒体音乐平台的发展，用户面临“歌曲多但难发现好歌”的问题。传统推荐主要依赖用户行为数据（点击、收藏、播放时长），但在新歌曲或新用户场景中存在冷启动问题。

  

本项目基于音频内容本身做数据挖掘，围绕以下目标开展：

  

1. 自动识别歌曲曲风（分类任务）。

2. 计算歌曲之间的内容相似度（检索任务）。

3. 在相似检索基础上形成可落地的推荐原型（推荐任务）。

  

项目数据来源为本地 `data/` 目录下的 MP3 音频文件（示例：`data/Gee.mp3`）。

  

---

  

## 2. 项目目标（对老师可直接展示）

  

### 2.1 总体目标

  

构建一个“音乐曲风识别 + 相似歌曲推荐”的小型数据挖掘系统。

  

### 2.2 可交付功能

  

1. 输入任意一首 MP3，输出该歌曲的曲风类别及概率。

2. 输入任意一首 MP3，输出 Top-K 相似歌曲列表。

3. （可选扩展）结合用户偏好记录，实现简化版个性化推荐。

  

### 2.3 预期效果

  

- 曲风分类模型达到可解释、可评估的结果（如 Accuracy/F1）。

- 相似检索结果与人类主观听感具有一定一致性。

- 系统具备初步落地价值（音乐 App 场景、短视频配乐场景等）。

  

---

  

## 3. 模块设计

  

系统按“数据层 -> 特征层 -> 模型层 -> 服务层”设计。

  

### 模块 A：数据采集与标注模块

  

- 输入：`data/` 下 MP3 文件。

- 处理：建立歌曲元数据表（文件名、时长、采样率、标签）。

- 输出：结构化清单（CSV/JSON）。

  

说明：由于原始数据可能没有统一曲风标签，需要进行人工半自动标注（例如按流行、说唱、R&B、古典、动漫、儿歌等类别）。

  

### 模块 B：预处理模块

  

- 音频解码：MP3 -> WAV。

- 统一采样率（如 22050 Hz）与单声道。

- 音量归一化，去除明显静音段。

- 固定窗口切片（如每 10s 一段，重叠 50%）。

  

目标：降低采样条件差异带来的噪声，提高特征可比性。

  

### 模块 C：特征工程模块

  

从每个音频切片提取以下特征：

  

1. 时域特征：Zero Crossing Rate（过零率）、RMS 能量。

2. 频域特征：Spectral Centroid、Bandwidth、Roll-off。

3. 感知特征：MFCC（及一阶、二阶差分）。

4. 和声特征：Chroma。

5. 节奏特征：Tempo/BPM、Beat 强度。

  

特征聚合方式：对切片级特征做统计汇总（均值、标准差、分位数），形成歌曲级向量。

- Zero Crossing Rate (ZCR, 过零率)  
  - 是什么：波形穿过 0 轴的频率（符号正负切换次数）。  
  - 听感直觉：声音越“噪”“碎”“尖”，过零率通常越高。  
  - 高/低含义：打击乐、嘶声、说唱辅音段常偏高；平滑长音、人声抒情段偏低。
- RMS Energy (均方根能量)  
  - 是什么：音频“响度/能量”的稳定度量。  
  - 听感直觉：这段声音有多“有劲”。  
  - 高/低含义：副歌、鼓点强的段落偏高；前奏、间奏、弱唱段偏低。
- Spectral Centroid (频谱质心)  
  - 是什么：频谱“重心”位置，常叫“明亮度”指标。  
  - 听感直觉：声音是偏“亮/尖”还是偏“暖/沉”。  
  - 高/低含义：镲片、电吉他高频多时偏高；低频厚、柔和段偏低。
- Spectral Bandwidth (频谱带宽)  
  - 是什么：频谱分布有多“宽”。  
  - 听感直觉：声音成分是集中还是分散。  
  - 高/低含义：复杂编曲、噪声成分多时偏高；单一乐器或纯净人声偏低。
- Spectral Roll-off  
  - 是什么：累计到某比例能量（常用 85%）时对应的截止频率。  
  - 听感直觉：高频“延伸”到多远。  
  - 高/低含义：高频丰富（亮、脆）偏高；偏闷、低频主导偏低。
- MFCC (Mel 频率倒谱系数)  
  - 是什么：最经典的音色特征，压缩了频谱形状，贴近人耳感知。  
  - 听感直觉：能区分“这是什么声音质地”（人声、乐器、音色风格）。  
  - 用途：分类里非常关键，通常是主力特征。  
  - 一阶差分：MFCC变化速度（音色怎么变）。  
  - 二阶差分：变化的变化（音色变化是否突然）。  
  - 直觉：有助于抓住旋律和发声动态，不只是静态音色。
- Chroma (和声特征)  
  - 是什么：把频率映射到 12 个音级（C, C#, D...），忽略八度，只看“音名类”。  
  - 听感直觉：这段和弦/调性在怎么走。  
  - 高/低含义：不是简单高低，而是“12维分布模式”；对区分和声风格、流行和古典差异很有用。
- Tempo / BPM (速度)  
  - 是什么：每分钟节拍数。  
  - 听感直觉：歌是快还是慢。  
  - 作用：对舞曲、抒情、说唱等风格区分有帮助，但不能单独决定曲风。
- Beat Strength (节拍强度)  
  - 是什么：节拍峰值的明显程度（节奏感“打点”有多强）。  
  - 听感直觉：有没有“点头感”、律动强不强。  
  - 高/低含义：鼓点明确的歌偏高；自由节奏或弱节拍段偏低。
---
聚合方式也很关键：
- 切片级（比如每 10 秒）先提特征，得到很多小段结果。  
- 再对每个特征做 均值/标准差/分位数，变成“歌曲级向量”。  
- 这样做的好处：  
  - 均值：整体风格水平  
  - 标准差：波动程度（比如副歌和主歌差异）  
  - 分位数：避免被极端片段带偏
  - 
一句话总结给你汇报可用：  
这些特征分别从音色、亮度、和声、节奏和动态变化多个维度，把主观听感量化成可计算向量，从而支持分类与推荐。

  

### 模块 D：挖掘与建模模块

  

1. 曲风分类（监督学习）：

- Baseline：SVM / Random Forest / XGBoost。

- 进阶：CNN + Log-Mel 频谱图。

2. 相似检索（无监督/度量学习思路）：

- 向量标准化后用余弦相似度或欧氏距离。

- 建立歌曲向量索引，实现 Top-K 近邻查询。

3. 可视化分析：

- PCA / t-SNE / UMAP 降维观察曲风聚类。

  

### 模块 E：推荐与展示模块

  

- 输入一首目标歌曲。

- 输出“同风格 + 高相似”的推荐列表。

- 提供简要可解释信息（例如“节奏接近、音色相近、和声结构相似”）。

  

---

  

## 4. 数据处理方案（具体步骤）

  

### 第一步：数据盘点

  

- 扫描 `data/` 目录，统计音频数量、时长分布、采样率分布。

- 清洗异常文件（损坏文件、时长过短文件）。

  

### 第二步：标签体系设计

  

- 先定义 5-8 个主类，避免类别过细导致样本稀疏。

- 建议类别示例：Pop、Rap/Hip-hop、R&B、Classical、Anime/OST、Children、Folk/Oldies。

- 人工标注并记录置信度（高/中/低）。

  

### 第三步：样本切片与增强

  

- 每首歌切成多个 10s 样本，提升训练样本量。

- 可选增强：轻微加噪、微小时移、音高微调（控制幅度）。

  

### 第四步：特征提取与特征库构建

  

- 使用 `librosa` 批量提取特征。

- 保存为 `features.csv` 或 `features.parquet`。

- 做缺失值处理、标准化（StandardScaler）。

  

### 第五步：训练与评估

  

- 划分训练/验证/测试集（建议按歌曲划分，避免切片泄露）。

- 训练分类器并输出 Accuracy、Precision、Recall、F1。

- 通过混淆矩阵分析易混淆类别。

  

### 第六步：相似检索与推荐验证

  

- 对每首歌生成向量并建立近邻索引（KNN/Faiss 可选）。

- 人工抽查若干查询样本，评估推荐合理性。

- 形成案例库（输入歌 -> 推荐列表 -> 主观解释）。

  

---

  

## 6. 分析与评估设计

  

### 6.1 分类任务评估

  

- 指标：Accuracy、Macro-F1（类别不均衡时更重要）。

- 图表：混淆矩阵、各类别 F1 柱状图。

- 分析点：哪些类别容易混淆，以及可能原因（节奏相近、配器相似）。

  

### 6.2 检索/推荐任务评估

  

- 离线指标：Precision@K、Recall@K（有标签时）。

- 主观评估：人工打分（相似度 1-5 分）。

- 解释性分析：展示 query 与推荐歌曲在 BPM/MFCC 维度的接近程度。

  



## 9. 预期难点与应对策略


1. 标签不标准：采用“主类优先、细类后置”的分层标签方案。

2. 类别不均衡：使用重采样、类别权重、Macro-F1 评价。

3. 样本规模有限：切片扩样 + 数据增强 + 先做传统模型基线。

4. 主观听感差异：引入人工评估表，补充定量指标。

