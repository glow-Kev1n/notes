## 判环
### 并查集（仅适用无向图）
```cpp
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;

const int N = 100010;

int p[N];
int n, m;

int find(int x) {
    return p[x] == x ? p[x] : p[x] = find(p[x]);
}
//并查集适合无向图的判环

int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i ++ ) p[i] = i;
    bool ok = false;
    while (m -- ) {
        int a, b;
        cin >> a >> b;
        a = find(a), b = find(b);
        if (a != b) p[a] = b;
        else {
            ok = true;
            cout << "有环!" << endl;
            return 0;
        }
    }
    cout << "无环" << endl;
    return 0;
}
```


### 有向图判环
#### topsort拓扑排序
```cpp
auto topsort = [&]() -> bool {
	int cnt = 0;
	int q[n];
	int hh = 0, tt = -1;
	
	for (int i = 0; i < n; i ++ )
		if (din[i] == 0)//入度为0的入队
			q[++ tt] = i;
  

	while (hh <= tt) {
		int u = q[hh ++ ];
		cnt ++ ;//统计出队元素个数
		
		for (int i = h[u]; ~i; i = ne[i]) {
			int j = e[i];
			din[j] -- ;
			if (din[j] == 0) q[++ tt] = j;
			//din作为入度信息的同时还是st数组
			//如果din < 0，说明这个元素肯定入队过了
		}
	}

	return cnt < n;//最后统计出队元素是否等于n
	//如果< n说明有环
	//如果= n说明无环

};
```

#### dfs
```cpp
// AC one more times
// nndbk
#include <bits/stdc++.h>
using namespace std;
 
const int N = 1e5 + 10;
vector <int> e[N];
int vis[N];
int n, m;
bool dfs(int u) {
    vis[u] = 1; // 1表示正在访问
    for (int v: e[u]) {
        if (vis[v] == 1) { //如果正在访问的点又被访问到则代表有环
            return false;
        } else if (vis[v] == 0) { // 0代表还没有访问
            if (!dfs(v)) {
                return false;
            }
        }
    }
    vis[u] = 2; // 2代表访问结束
    return true;
}
 
bool check(){
    for (int i = 1; i <= n; i++) {
            if (vis[i] == 0) {
                if (!dfs(i)) {
                    return false;
                }
            }
        }
        return true;
    };
 
int main()
{
 
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u,v;  cin >> u >> v;
        e[u].push_back(u);
    }
    memset(vis, 0, sizeof(vis));
    
    puts(check() ? "no have" : "have");
    return 0;
}
```